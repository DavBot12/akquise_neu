🚀 Build a full-stack real estate acquisition tool using **Node.js**, **Playwright**, and **SQLite**, with a full working **web interface**.

---

## ✅ 1. Web Scraper (Playwright):

- Use **Playwright in Node.js** to scrape real estate listings from Willhaben.at.
- Target only **private listings** (not from real estate agents).
- Focus on:
  - Eigentumswohnungen in Wien  
    https://www.willhaben.at/iad/immobilien/eigentumswohnung/eigentumswohnung-angebote/wien
  - Grundstücke in Wien  
    https://www.willhaben.at/iad/immobilien/grundstuecke/grundstuecke-angebote/wien
  - Eigentumswohnungen in Niederösterreich  
    https://www.willhaben.at/iad/immobilien/eigentumswohnung/eigentumswohnung-angebote/niederoesterreich
  - Grundstücke in Niederösterreich  
    https://www.willhaben.at/iad/immobilien/grundstuecke/grundstuecke-angebote/niederoesterreich

---

### 🔎 Scraper Requirements:

- Go through multiple pages (start at page 1, stop when no more results)
- Detect if listing is from **"Privat"** (e.g. "Anbieter: Privat" or other heuristics)
- For each valid listing, scrape:
  - Title
  - Price (€)
  - Location
  - Area in m²
  - €/m² (calculated)
  - Full description
  - All image URLs
  - Listing URL
  - Scraped timestamp

- Save the data in **SQLite** with a field `akquise_erledigt` (boolean, default `false`)

---

## 💾 2. Database (SQLite):

- Table: `listings`
  - id, title, price, location, area, eur_per_m2, description, images (as array), url, scraped_at, akquise_erledigt
- Table: `contacts`
  - id, name, company, phone, email, notes
- Optional: table to assign listings to contacts (many-to-many)

---

## 🧠 3. Price Evaluation Logic:

- Calculate average €/m² per region (e.g. by Bezirk/district)
- Tag listings with:
  - `unter dem Schnitt`, `im Schnitt`, or `über dem Schnitt`
- Show tag in UI and color-code cards accordingly

---

## 🖥️ 4. Web Interface:

- A dashboard page with all **non-erledigt** listings
- Each card includes:
  - Title, image, location, €/m², description
  - Tag (e.g. "✅ günstig" or "❗ zu teuer")
  - Button: "Akquise erledigt" → sets flag in DB and hides it

- A separate **Scraper Console Tab**:
  - Shows scraper log output (console or textarea)
  - Includes "Jetzt scrapen" button to start scraping manually

---

## 📇 5. Contacts Tab:

- Add/Edit/Delete real estate developer contacts ("🏗️ Wichtige Kontakte")
- Fields: Name, Company, Email, Phone, Notes
- Manually assign listings to contacts

---

## 📦 Final Setup:

- **Backend**: Node.js (Express)
- **Scraper**: Node.js with Playwright
- **Database**: SQLite
- **Frontend**: HTML/CSS + JS or optionally React
- File Structure:
